export const VIEW_PATHS = { home: "/inicio", timeline: "/tempo/horizontal", vertical: "/tempo/vertical", "philosophy-tree": "/tempo/arvore", "knowledge-tree": "/conhecimento/evolucao", "knowledge-map": "/conhecimento/mapa", profiles: "/perfis", map: "/mapa", about: "/sobre", admin: "/administracao" };
export function routeFromPath(pathname) { const path = pathname === "/" ? "/inicio" : pathname.replace(/\/$/, "") || "/inicio"; const profile = path.match(/^\/perfis\/(\d+)$/); if (profile) return { view: "profiles", entityId: Number(profile[1]) }; const match = Object.entries(VIEW_PATHS).find(([, candidate]) => candidate === path); return { view: match?.[0] || "home", entityId: null }; }
export function pathForView(view, entityId = null) { return view === "profiles" && entityId ? `/perfis/${entityId}` : VIEW_PATHS[view] || VIEW_PATHS.home; }
